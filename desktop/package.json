{
  "name": "shield-desktop",
  "version": "1.0.23",
  "description": "Desktop shell for Shield",
  "main": "main.cjs",
  "private": true,
  "author": "",
  "license": "UNLICENSED",
  "scripts": {
    "start": "electron .",
    "pack": "electron-builder --dir",
    "predist": "node build/validate-production-config.cjs",
    "dist": "electron-builder --win nsis",
    "dist:unsigned": "electron-builder --win nsis -c.win.signAndEditExecutable=false",
    "predist:portable": "node build/validate-production-config.cjs",
    "dist:portable": "electron-builder --win portable"
  },
  "devDependencies": {
    "electron": "^33.4.11",
    "electron-builder": "^25.1.8",
    "rcedit": "^5.0.2"
  },
  "build": {
    "icon": "build/icon.ico",
    "afterPack": "build/after-pack.cjs",
    "appId": "com.shield.desktop",
    "productName": "Shield",
    "directories": {
      "output": "release"
    },
    "files": [
      "main.cjs",
      "preload.cjs",
      "build/icon.png",
      "build/icon.ico",
      "config*.json",
      "README.md"
    ],
    "publish": [
      {
        "provider": "generic",
        "url": "${env.SHIELD_UPDATE_URL}"
      }
    ],
    "win": {
      "signAndEditExecutable": true,
      "icon": "build/icon.ico",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "Shield-Setup.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Shield",
      "installerIcon": "build/icon.ico",
      "uninstallerIcon": "build/icon.ico"
    }
  },
  "dependencies": {
    "electron-updater": "^6.8.9"
  }
}
